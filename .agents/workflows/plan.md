# Plan Workflow

Use the `dandi-plan` skill.

## Steps

1. Read `AGENTS.md`, `docs/PROJECT_RULES.md`, and relevant shared docs.
2. Inspect only files needed to make the plan concrete.
3. Break work into safe phases.
4. Define acceptance criteria and validation using commands from `package.json`.
5. Produce execution prompts when useful.

## Boundary

Read-only. Do not edit, stage, commit, or push.
